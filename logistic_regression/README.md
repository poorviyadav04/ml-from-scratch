# Logistic_Regression_with_a_Neural_Network
This project implements a Logistic Regression classifier from scratch using NumPy to classify images (or general binary datasets). The model is optimized using gradient descent, and performance is evaluated across different learning rates to study their impact on convergence speed and accuracy.
